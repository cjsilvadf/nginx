---
- hosts: all
  gather_facts: true
  become: true
  become_user: root
  tasks:
    - name: Reboot host and wait for it to restart
    - name: update the system
      yum:
        name: "*"
        state: latest
